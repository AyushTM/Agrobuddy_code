<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Agrobuddy" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Night Mode -->
    <!-- Screen backgroud if night mode on -->
    <style name="SCREEN" >
        <item name="android:background">#3c4045</item>
    </style>
    <!-- Text color if night mode on -->
    <style name="TEXT">
        <item name="android:textColor">#FFF</item>
    </style>
    <style name="TEXT2">
        <item name="android:textColor">#ff635b</item>
    </style>
</resources>